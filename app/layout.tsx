import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Weather Dashboard - Real-time Weather Information",
  description: "A modern weather dashboard built with Next.js, React, and TypeScript. Get real-time weather data for any city worldwide. Features beautiful UI, search history, and comprehensive weather details.",
  keywords: ["weather", "dashboard", "nextjs", "react", "typescript", "weather app", "forecast"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
